#ifndef TYPEEXCEPTION.HPP
#define TYPEEXCEPTION.HPP

#include <stdexcept>
#include <exception>
#include <string>

struct TypeException : std::exception {
public:
  const char* what() const throw() {
    return "Type exception.";
  }
}
#endif
